<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="sms_template_pos_receipt" model="sms.template">
            <field name="name">POS Receipt SMS</field>
            <field name="model_id" ref="point_of_sale.model_pos_order"/>
            <field name="body">Receipt for Order: ${object.name}
Date: ${object.date_order.strftime('%d-%m-%Y %H:%M')}
Company: ${object.company_id.name}
% if object.partner_id.name:
Customer: ${object.partner_id.name}
% endif

Items:
% for line in object.lines:
- ${line.product_id.name} x${line.qty} = ${line.price_subtotal_incl} kr
% endfor

Total: ${object.amount_total} kr

Thank you for your purchase!</field>
        </record>
    </data>
</odoo>